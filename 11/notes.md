primitive expressions: the simplest entities the language is concerned with, or computes
means of combination: greater elements are made from the primitive ones
means of abstraction: the transformation of compound elements as units

Read: takes in written code
Eval: preforms computations
 - Eval must run the "evaluation process" on every element???
 - First evaluate the sub-expressions of the combination (numbers and operations(?))
 - Then apply the procedure according to what is already evaluated..?

 - Eval has "pre stipulations," like the fact that numbers return numbers, operators perform their respective commands, and variables may act as a combination of numb/oper
 - define Does not cover these definitions. It is not considered a "combination"; it is known as a "special form"

Print: outputs the result
Loop:repeats for more entries.

What does it mean to be in a "global environment"? Is that different from "the environment?"
Lisp (or scheme)(or racket) is like English. There are many concrete, general rules, with random exceptions sprinkled throughout. 
